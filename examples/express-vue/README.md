# Express + Vue 3 + Inertia Example

Run two processes:

```bash
pnpm install
pnpm dev   # starts vite + tsx server concurrently
```

Visit http://localhost:3000 — Vite serves the client at :5173, the server embeds the page object in the v3 `<script type="application/json" data-page="app">` tag.
